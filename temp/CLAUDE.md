# temp

Project-specific guidance for `temp`. The root `CLAUDE.md` covers repo-wide
conventions (external deps, build/flash/serial console commands) that apply
here too — Claude Code loads both when you're working in this directory.

This project doesn't have a defined purpose yet — fill in the sections below
once it does. "temp" is likely a placeholder name; consider renaming the
directory once the project's real purpose is settled.

## Current status

- **Last updated:** 2026-07-10
- **Doing now:** not started — directory and CLAUDE.md stub only, no source
  or CMake project yet.
- **Known gap:** n/a
- **Next up:** define what this project is for, then scaffold it following
  the pattern used by the other projects (`CMakeLists.txt`,
  `pico_sdk_import.cmake`, `wbuild.sh`/`xbuild.sh`, `load_temp.sh`,
  `start_minicom.sh` — see any sibling project, e.g. `../rtc/`, for the
  template).

## Architecture

(not yet defined)
